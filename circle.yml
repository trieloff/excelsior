machine:
  environment:
    DYNAMODB_ENDPOINT: https://dynamodb.us-east-1.amazonaws.com
deployment:
  production:
    branch: master
    commands:
    - lein deploy private
  staging:
    branch: lambda-deploy
    commands:
    - lein lambda update dev
