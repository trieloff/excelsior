machine:
  environment:
    dynamodb_endpoint: https://dynamodb.us-east-1.amazonaws.com
deploy:
  production:
    branch: master
    commands:
    - lein deploy private
  staging:
    branch: private-deploy
    commands:
    - lein deploy private
